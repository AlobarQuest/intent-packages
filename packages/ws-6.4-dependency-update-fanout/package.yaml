schema_version: 1
package_id: ws-6.4-dependency-update-fanout
title: "WS-6.4 — sequentially re-prove one systemic dependency update across six repositories, autonomously up to the merge gate"
revision: 8
status: approved
created_by: claude-code-interactive
owner: devon
created_at: "2026-07-09T21:57:24Z"
supersedes: null
outcome:
  what: "Re-prove the six repositories sequentially under the repaired dependency-update authority contract. Revision 8 realizes only the AC-004 target: one InfraOps MCP Server work unit updates the vitest pinned dependency from 4.1.4 to 4.1.10 at its single package.json pin site (devDependencies), regenerates package-lock.json, opens one pull request, and proves the named check green while Devon retains every approval and merge decision."
  why: "Historical 6/6 completion labels are not lifecycle or evidence authority. Each repository must be separately re-proven under the repaired, fingerprinted mutation_commands contract before its acceptance criterion can be treated as realized."
  beneficiary: "Devon gets a bounded, reviewable proof that the repaired factory path works on an npm/package.json repository (InfraOps MCP Server) — the first exercise of the npm tooling profile after the pip and uv profiles were proven in revisions 4 through 7 — before the remaining unproven repository is attempted."
  success_signal: "Exactly one AC-004 InfraOps MCP Server unit is created from a Devon-approved revision and decomposition; its approved authority envelope permits only the exact preflight-proven command sequence, moving the vitest pin so package.json and its lockfile stay consistent; its pull request's named check reports the repository's full gate green on the exact head; the WS-5.1 verifier adjudicates the evidence; and no agent, workflow, API, CLI, or orchestrator path approves in batch or merges anything."
scope:
  included:
    - "Author and approve this intent package before any work unit is created."
    - "Treat all historical 6/6 completion claims as non-authoritative; revision 4 proved AC-006 for Change Manager, revision 5 proved AC-001 for Orchestrator, revision 6 proved AC-002 for Brain, and revision 7 proved AC-003 for Security Standards. Revision 8 maps only AC-004 to InfraOps MCP Server while AC-005 and AC-007..AC-011 remain retained and incomplete."
    - "Before authoring the InfraOps MCP Server authority envelope, prove it against the real target repository: confirm a real dependency upgrade is available (vitest 4.1.4 to 4.1.10), confirm the ordered allowed_commands mutator yields a real diff on the target's package.json and package-lock.json, confirm the mutator is idempotent under a second execution, and confirm the pin is single-site (devDependencies) so no lockfile or sibling section is left behind."
    - "Create exactly one Draft InfraOps MCP Server work unit through the WS-3.2 decomposition path, so that the unit is stamped with its own constraints.work_unit_id."
    - "Authorize the complete InfraOps MCP Server outcome with target_repository AlobarQuest/infraops-mcp-server, change_class dependency-update, required_capability repo.edit, allowed_commands ordered as the single mutator npm install vitest@4.1.10 --save-exact --save-dev then the verifier grep -q '\"vitest\": \"4.1.10\"' package.json, and mutation_commands containing only that npm install mutator."
    - "Attest conformance for the InfraOps MCP Server unit from the target repository's real state via security_scan.cli.scan and portfolio.compliance.build_rows."
    - "Record one named human authority approval bound to the InfraOps MCP Server unit's own authority fingerprint."
    - "Prove the WS-4.2 kill switch blocks the InfraOps MCP Server unit on live production before the revision-8 target scope is enabled."
    - "Replace the previous Security Standards production target scope with the single target AlobarQuest/infraops-mcp-server, while retaining only change class dependency-update and capability repo.edit, after Devon's explicit approval."
    - "Dispatch InfraOps MCP Server once and observe admission, target routing, idempotency, and failure-signature circuit-breaker behavior."
    - "Let factory-runner open one InfraOps MCP Server pull request that updates package.json and package-lock.json and runs the named check."
    - "Submit AC-004 evidence and let the WS-5.1 verifier adjudicate it."
    - "Record the InfraOps MCP Server dispatch identifier, pull-request URL, exact-head named-check result, verifier outcome, and guardrail observations without projecting them onto the retained acceptance criteria."
  excluded:
    - "Any automatic merge, or any worker, agent, API, CLI, CI, or orchestrator path that merges a pull request."
    - "Any automatic Brain approval or knowledge promotion."
    - "Tracker canonicalization: no Linear, Todoist, or GitHub state becomes canonical lifecycle state."
    - "Graduation automation beyond the one Devon-approved change-class enablement."
    - "Automatic follow-up generation from this package's follow_up block."
    - "New observation collectors."
    - "Validation-harness work or any new validation machinery."
    - "Batch approval of work-unit authority, which would collapse N named human approvals into one."
    - "Any further authority-envelope contract expansion not already approved and merged as part of the coordinated mutation_commands contract revision."
    - "Dependency updates in AlobarQuest/factory-runner itself, which has no dependabot configuration and is excluded from the target set."
  non_goals:
    - "Proving that dependency updates are correct; the repositories' own checks and Devon's review decide that."
    - "Making dispatch automation permanent; the standing posture after this fan-out is a separate Devon decision."
    - "Extending the fan-out beyond the change class dependency-update."
    - "Building queueing, batching, or scheduling machinery for fan-out units."
  assumptions:
    - "The orchestrator authenticates to GitHub as the Alobar SDS Dispatch app, installed on InfraOps MCP Server, minting hourly installation tokens."
    - "InfraOps MCP Server hosts the reviewed caller workflow .github/workflows/factory-runner-pilot.yml on its default branch and carries the required Actions secret names (FACTORY_RUNNER_TOKEN, FACTORY_RUNNER_CREDENTIAL_KEY_ID, ANTHROPIC_API_KEY, FACTORY_PR_TOKEN); factory-runner's active reusable workflow at main installs and runs the factory-runner console script. This is verified against InfraOps MCP Server before dispatch enablement."
    - "AlobarQuest/factory-runner is public, so a caller's GITHUB_TOKEN can install it, and its reusable workflow invokes the installed console script rather than a repository-local script."
    - "Current InfraOps MCP Server conformance will be computed from live repository state before proposal; no status or accepted_standards value is carried forward from historical evidence."
    - "Devon alone approves this package revision, approves the decomposition, approves each work unit's authority, approves enabling dispatch, and merges every pull request."
  open_questions: []
sources:
  - location: "Devon's live 2026-07-09 instruction to resume Phase 6, run the systemic fan-out across six repositories, and hold the merge gate permanently"
    authority_level: authoritative
    required_version: "2026-07-09"
    trust: trusted_instruction
    sensitivity: internal
  - location: "~/docs/software-delivery-system/2026-07-02-software-factory-master-plan.md"
    authority_level: authoritative
    required_version: "2026-07-02"
    trust: trusted_instruction
    sensitivity: internal
  - location: "~/docs/software-delivery-system/2026-07-09-mvp-closeout-plan.md"
    authority_level: authoritative
    required_version: "2026-07-09"
    trust: trusted_instruction
    sensitivity: internal
  - location: "~/Projects/orchestrator/docs/decisions/0001-work-unit-authority-envelope-contract.md"
    authority_level: authoritative
    required_version: "2026-07-09"
    trust: trusted_instruction
    sensitivity: internal
  - location: "~/docs/software-delivery-system/2026-07-09-ws64a-deploy-and-onboarding-state.md"
    authority_level: supporting
    required_version: "2026-07-09"
    trust: untrusted_data
    sensitivity: internal
  - location: "~/docs/software-delivery-system/2026-07-09-ws640c-github-app-state.md"
    authority_level: supporting
    required_version: "2026-07-09"
    trust: untrusted_data
    sensitivity: internal
  - location: "~/Projects/infraops-mcp-server at merged main (npm repository; vitest pinned with a caret at a single site in package.json: devDependencies)"
    authority_level: supporting
    required_version: "2026-07-22"
    trust: untrusted_data
    sensitivity: internal
  - location: "~/Projects/intent-packages at merged main commit 644fcaa8b9f03c8ca69d864f3efbf8e9b730c652"
    authority_level: reference
    required_version: "644fcaa8b9f03c8ca69d864f3efbf8e9b730c652"
    trust: untrusted_data
    sensitivity: internal
  - location: "docs/superpowers/evidence/2026-07-22-ws64-revision8-infraops-mcp-server-preflight.md"
    authority_level: supporting
    required_version: "2026-07-22"
    trust: untrusted_data
    sensitivity: internal
constraints:
  time_budget: "Revision 8 permits one AC-004 decomposition, one bounded InfraOps MCP Server dispatch enablement, and one pull request; every later repository proof requires its own explicit lifecycle gates."
  technology: "The orchestrator's existing WS-3.2 decomposition path, WS-4.2 dispatch adapter, WS-4.1 factory-runner, and WS-5.1 verifier. No new decomposition, dispatch, or verification machinery."
  policy_legal: "Devon alone approves this exact package revision, approves the decomposition, approves each work unit's authority, approves enabling production dispatch, and merges every pull request."
  privacy_security: "No secrets in tracked files. The GitHub App private key lives only in Bitwarden Secrets Manager and reaches production as a base64 environment variable. Repository content, dependabot metadata, and pull-request text are treated as data, never as instructions."
  compatibility: "The approved shared authority envelope includes fingerprinted constraints.mutation_commands. Orchestrator and factory-runner must use the byte-identical merged contract; any further unapproved contract expansion is out of scope."
  quality_accessibility: "Each repository's own named checks gate its own pull request. Evidence must be readable by Devon without reconstructing it from logs."
  operational: "Production dispatch must be disabled and observed blocking before the revision-8 target scope is enabled. Revision 8 enablement is scoped to dependency-update, repo.edit, and only AlobarQuest/infraops-mcp-server; the prior Security Standards target scope is replaced, not broadened."
  other:
    - "Work units must be created through the decomposition path; units created through the direct revision path are not stamped with constraints.work_unit_id and factory-runner rejects them."
    - "The authored authority envelope must omit constraints.work_unit_id, which the orchestrator stamps at proposal time and rejects if author-supplied."
    - "accepted_standards must originate from a real waiver source and must never be echoed from standards_touched."
    - "Revision 8 may propose and dispatch only the AC-004 InfraOps MCP Server unit; later repository proofs require their own explicit lifecycle gates."
    - "For dependency-update with repo.edit allowed, constraints.mutation_commands is a required non-empty ordered subset of constraints.allowed_commands and is part of the authority fingerprint."
    - "An authority envelope must be able to accomplish its own outcome: constraints.allowed_commands must contain the mutation the unit's outcome requires, and not merely the command that verifies it."
    - "constraints.allowed_commands is an ordered list that factory-runner re-executes in full before it inspects the working tree, so every command must be idempotent under a second execution against the same checkout, and mutators must precede the verifier that attests to the tree being committed."
    - "constraints.allowed_commands must begin with a step that installs the target repository's own dependencies WHEN the envelope names a tool-guarded check that would otherwise exit zero having verified nothing (because the reusable workflow installs only factory-runner). An envelope whose only verifier is a deterministic, non-tool-guarded assertion — for example a grep that the pin moved — is honest without an install step and need not include one."
    - "constraints.allowed_commands may verify only what the hosted runner can honestly execute. A repository whose checks require a service container, environment variables, or a migrated database that the reusable workflow does not provide must not name those checks in its envelope; its tests are verified by its own named check on the pull-request head, which is where this package's acceptance criteria already place that evidence."
    - "Before an envelope names a verifier, that verifier must be run against an unmodified clean clone of the target repository in a bare environment, so that a failure caused by the runner's environment is never mistaken for a failure caused by the dependency update."
    - "A unit outcome that directs an edit to a pinned dependency must name every site of that pin by file and section — for a uv repository because uv resolves dependency groups and extras jointly and a partial pin edit renders the lockfile unsatisfiable, for a pip/requirements.txt repository because a pin duplicated across requirements.txt and requirements-dev.txt must move together or the environment is left inconsistent, and for an npm/package.json repository because a package listed in both dependencies and devDependencies must move at every listed site."
acceptance:
  - id: AC-001
    condition: "AlobarQuest/orchestrator, the canary, receives a factory-runner pull request that updates its pinned dependencies, and its named checks pass on the pull-request head."
    evidence_type: automated_check
    evidence: "Proven by revision 5: Orchestrator unit e252d942-636e-5a30-8038-a48689adb1be completed with verifier adjudication 6e9381f7-9352-462e-846d-1752f56fd29d on exact pull-request head 18a56d7f20fbb3e6ab8ecd1e6dc9051d0bfe487b; Devon then merged pull request 59 as e5728db."
    approver: policy
  - id: AC-002
    condition: "AlobarQuest/brain receives a factory-runner pull request that updates its pinned dependencies, and its named checks pass on the pull-request head."
    evidence_type: automated_check
    evidence: "Proven by revision 6: Brain unit 9c7c4df1-0d93-5dc2-960b-23b62d8b687b opened pull request 28 whose named check passed on exact head 26e505a0ab7c8744c92b31871e23e1825995585c; the WS-5.1 verifier adjudicated the evidence; Devon then merged pull request 28 as 6d2c08c524775fc4a816071a4f9635af3c91ef9b on 2026-07-17."
    approver: policy
  - id: AC-003
    condition: "AlobarQuest/security-standards receives a factory-runner pull request that updates its pinned dependencies, and its named checks pass on the pull-request head."
    evidence_type: automated_check
    evidence: "Proven by revision 7: Security Standards unit 38d3e90f-d8d7-5a8d-8b48-90b279f06c01 opened pull request 22 whose named check (Lint, type-check, and test) passed on exact head ca8e8c9da10547eaa09e531450ebdb81638c7384; the WS-5.1 verifier adjudicated the evidence (adjudication baf399cb-972f-4faa-8351-1cf7bfeb3970); Devon then merged pull request 22 as b46f5c37 on 2026-07-22."
    approver: policy
  - id: AC-004
    condition: "AlobarQuest/infraops-mcp-server receives a factory-runner pull request that updates its pinned dependencies, and its named checks pass on the pull-request head."
    evidence_type: automated_check
    evidence: "Revision 8 target only; incomplete until the InfraOps MCP Server pull request's named check (Lint, type-check, and test) succeeds on the exact head commit, the dispatch record names AlobarQuest/infraops-mcp-server, and the WS-5.1 verifier adjudicates the evidence."
    approver: policy
  - id: AC-005
    condition: "AlobarQuest/intent-packages receives a factory-runner pull request that updates its pinned dependencies, and its named checks pass on the pull-request head."
    evidence_type: automated_check
    evidence: "Retained and incomplete in revision 8; requires a separate Intent Packages proof whose pull request named check succeeds on the exact head commit and whose dispatch record names this repository."
    approver: policy
  - id: AC-006
    condition: "AlobarQuest/change-manager receives a factory-runner pull request that updates its pinned dependencies, and its named checks pass on the pull-request head."
    evidence_type: automated_check
    evidence: "Proven by revision 4: Change Manager unit 4c8c2af4-f963-5511-b3c3-330da81f6373 completed with verifier adjudication 7b3d434d-bc28-444a-8268-88380b4b90a3 on exact pull-request head a8297cf18c76549295bf16ae32466fa40e15f19e; Devon then squash-merged pull request 26 as 3f543fdd59942c042caeeb7899968ff04cac5291."
    approver: policy
  - id: AC-007
    condition: "Every work unit in this fan-out carries its own named human authority approval bound to that unit's own authority fingerprint, and no batch approval path was created or used."
    evidence_type: automated_test
    evidence: "Retained and incomplete in revision 8; requires separately approved units whose authority_approval_id values are present and distinct and whose fingerprints reflect their own target_repository and server-stamped work_unit_id."
    approver: policy
  - id: AC-008
    condition: "Before each repository's target scope is enabled, dispatching that revision's ready work unit while the kill switch is disabled records a skipped dispatch with reason code dispatch_disabled and never calls GitHub."
    evidence_type: automated_check
    evidence: "Retained and incomplete in revision 8; revision 8 requires a fresh live dispatch record with status skipped and reason_code dispatch_disabled before InfraOps MCP Server target enablement."
    approver: policy
  - id: AC-009
    condition: "Production dispatch is enabled for exactly one change class, one capability, and one target repository at a time; after the completed Orchestrator, Change Manager, Brain, and Security Standards proofs, the InfraOps MCP Server unit is dispatched and adjudicated before the remaining one unproven repository."
    evidence_type: human_review
    evidence: "Retained and incomplete in revision 8; requires Devon to confirm each sequential one-target enablement scope and that the required guardrail observations preceded dispatch."
    approver: devon
  - id: AC-010
    condition: "The WS-5.1 verifier adjudicates each unit's evidence; no worker, agent, or dispatch path declares a unit complete."
    evidence_type: automated_test
    evidence: "Retained and incomplete in revision 8; requires every sequential unit's completion to be attributable to a verifier actor and lifecycle guards to reject worker-declared completion."
    approver: policy
  - id: AC-011
    condition: "Devon merges every pull request personally; no agent, workflow, CI job, orchestrator path, or GitHub App permission merges anything, and merge_to_main is never projected into a work-unit authority envelope."
    evidence_type: human_review
    evidence: "Retained and incomplete in revision 8; requires Devon to confirm every merge is his own and the served authority envelopes contain no merge capability."
    approver: devon
authority:
  allowed:
    - repository_read
    - repository_write
    - test_execution
    - pr_open
    - event_emit
  requires_approval:
    - merge_to_main
    - secret_read
    - secret_write
    - infra_mutation
    - credential_create
    - credential_revoke
  prohibited:
    - outward_publish
    - email_send
    - spend_money
    - calendar_write
    - third_party_contact
    - data_delete
    - external_system_write
  budgets:
    max_attempts: 3
    max_llm_calls: 4
deliverables:
  artifacts:
    - "One Draft InfraOps MCP Server work unit created from a one-unit AC-004 decomposition proposal"
    - "One InfraOps MCP Server pull request opened by factory-runner"
    - "The InfraOps MCP Server dispatch record plus the fresh kill-switch skipped record that precedes it"
    - "The AC-004 verifier adjudication"
    - "Evidence that AC-001, AC-002, AC-003, and AC-006 remain proven while AC-005 and AC-007..AC-011 remain retained and incomplete"
  destination: "InfraOps MCP Server receives the revision-8 pull request; lifecycle records live in the orchestrator at sds.alobar.net; this governing package remains in AlobarQuest/intent-packages."
  recipient: devon
  definition_of_done: "Revision 8 is complete only when AC-004 has realized evidence and verifier adjudication, Devon has personally merged or closed the InfraOps MCP Server pull request, AC-001, AC-002, AC-003, and AC-006 remain proven, the other criteria remain explicitly incomplete, and no permanent gate was weakened. The six-repository proof is not complete at that point."
  operator_responsibilities:
    - "Approve or reject this exact intent revision and hash."
    - "Approve the one-unit AC-004 decomposition proposal."
    - "Approve the InfraOps MCP Server work unit's authority envelope by its own fingerprint."
    - "Approve replacing the production target scope with only dependency-update, repo.edit, and AlobarQuest/infraops-mcp-server."
    - "Review and merge the InfraOps MCP Server pull request personally."
    - "Decide the standing dispatch posture after the InfraOps MCP Server proof."
dependencies:
  predecessor_packages:
    - package: ws-3.2-package-intake-decomposition
      revision: 1
  external_decisions:
    - "Devon approval of this exact package revision before any work unit is created."
    - "Devon approval to replace the production target scope with dependency-update, repo.edit, and only AlobarQuest/infraops-mcp-server, kill-switch-verified first."
    - "Devon decision on the standing dispatch posture after the InfraOps MCP Server proof."
  required_people_systems:
    - "Devon for revision-8 package approval, one-unit decomposition approval, InfraOps MCP Server authority approval, dispatch enablement, pull-request review, and merge"
    - "The orchestrator at sds.alobar.net as canonical lifecycle owner"
    - "AlobarQuest/factory-runner as the WS-4.1 worker, invoked by workflow_dispatch"
    - "The Alobar SDS Dispatch GitHub App, installed on InfraOps MCP Server"
    - "GitHub Actions for the InfraOps MCP Server named check"
    - "Alobar ID forward-auth for every human action"
  required_capabilities:
    - repository_read
    - repository_write
    - test_execution
    - pr_open
    - event_emit
  blocking_conditions:
    - "This exact package revision is not approved and chain-verified."
    - "The orchestrator running in production cannot mint a GitHub App installation token."
    - "The kill switch has not been observed blocking a dispatch on live production."
    - "Devon has not explicitly approved enabling production dispatch for the change class."
    - "Any work unit lacks its own named human authority approval."
    - "Any work unit's conformance was hand-typed, or its accepted_standards echoes its standards_touched."
    - "A work unit was created through the direct revision path rather than the decomposition path."
    - "Any path exists by which a worker, agent, workflow, or the orchestrator could merge a pull request."
risk:
  failure_modes:
    - "A unit is dispatched to the wrong repository, opening a dependency pull request against a repository it was not authorized for."
    - "Batch approval is introduced to reduce the friction of sequential named authority approvals, collapsing the per-unit human gate."
    - "Conformance is hand-typed or accepted_standards echoes standards_touched, turning the admission gate into a tautology that admits everything."
    - "Dispatch is enabled for more than dependency-update and repo.edit, or for any target other than AlobarQuest/infraops-mcp-server."
    - "The kill switch is enabled before it has been observed blocking anything, so the guardrail is asserted rather than proven."
    - "A dependency pull request is merged by automation, or a GitHub App permission grants a merge path."
    - "The InfraOps MCP Server pull request collides with in-flight work in that repository, which currently has eight open Dependabot pull requests; the factory branch and the vitest bump do not collide with any of them, but a later Dependabot merge could conflict at Devon's merge gate."
    - "An authority envelope is authored that cannot accomplish its own outcome, so a well-behaved agent obeys its authority, changes nothing, exits successfully, and the run dies at the change-detection guard, consuming a package revision and every named human approval bound to it."
    - "An envelope command that succeeds in the coding phase fails when factory-runner re-executes the ordered list at finalize, so the unit can never open a pull request no matter how many attempts it is granted."
    - "A retired or mis-authored work unit is left in a state from which no actor can retire it, and is later dispatched by an operator who mistook it for its corrected replacement."
  max_impact: "One unwanted InfraOps MCP Server pull request that Devon closes. Dispatch cannot merge, cannot mutate production infrastructure, cannot write secrets, and is bounded to repo.edit, dependency-update, and AlobarQuest/infraops-mcp-server."
  stop_conditions:
    - "Routine local invocation mistakes are recoverable after refreshing state and proving they did not mutate factory state."
    - "Stop when the factory process behaves incorrectly, including admission, routing, idempotency, circuit-breaker, command-enforcement, result-classification, evidence, or lifecycle behavior that differs from the approved contract."
    - "The InfraOps MCP Server dispatch record names any repository other than AlobarQuest/infraops-mcp-server."
    - "A work unit reaches a dispatchable state without a named human authority approval."
    - "The approved package hash changes without a new revision and renewed approval."
    - "Any change would create an automatic merge, automatic Brain approval, tracker canonicalization, graduation automation, or automatic follow-up generation."
  rollback: "Disable dispatch by unsetting ORCHESTRATOR_DISPATCH_ENABLED and restarting; close the unwanted InfraOps MCP Server pull request; cancel the affected work unit. Revision-8 dispatch mutates only an InfraOps MCP Server feature branch, never its default branch, and never production infrastructure."
  escalation_target: devon
verification:
  independent_review:
    - "The WS-5.1 verifier adjudicates each unit's evidence independently of the worker that produced it."
    - "Each target repository's own named checks gate its own pull request."
    - "Devon reviews each pull request before merging it."
  non_mechanical:
    - "Whether the InfraOps MCP Server guardrail observations are strong enough to justify treating AC-004 as realized."
    - "Whether the InfraOps MCP Server dependency update is desirable right now, which is a judgment the checks cannot make."
    - "Whether the friction of sequential named authority approvals is the correct price for the per-unit human gate."
follow_up:
  required: true
  revisit_when: "After the InfraOps MCP Server pull request is merged or explicitly closed and AC-004 is adjudicated, before selecting the next unproven repository proof."
  signals:
    - "A named human authority approval was felt to be a formality rather than a decision"
    - "A dependency update needed judgment that no acceptance criterion represented"
    - "The dispatch guardrails behaved differently in production than in their tests"
    - "The standing dispatch posture decision proves difficult to reverse"
  owner: devon
applicable_standards:
  project: "1.0"
  code: "1.0"
  security: "1.0"
